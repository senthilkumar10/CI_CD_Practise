# Pull base image 
From tomcat:8.0-alpine

# Maintainer 
LABEL maintainer="senthilkumar10@gmail.com" 
RUN rm -fr /usr/local/tomcat/webapps/*.war
ADD ./webapp/target/webapp.war /usr/local/tomcat/webapps/
EXPOSE 8080
CMD ["catalina.sh", "run"]
